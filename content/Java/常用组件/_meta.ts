import type {MetaRecord} from 'nextra'

const meta: MetaRecord = {
    'JDBC': 'JDBC',
    'Jedis': 'Jedis',
    'Lombok': 'Lombok',
    'javap命令使用': 'javap命令使用',
    'sortspec': {
        display: 'hidden'
    }
}

export default meta
